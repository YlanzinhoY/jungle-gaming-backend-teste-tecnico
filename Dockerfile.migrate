FROM migrate/migrate:v4.18.3

# psql performs the one-time compatibility adjustment for databases created
# before this project adopted golang-migrate.
RUN apk add --no-cache postgresql-client

COPY scripts/migrate.sh /usr/local/bin/migrate-project
RUN chmod +x /usr/local/bin/migrate-project

ENTRYPOINT ["migrate-project"]
CMD ["up"]
